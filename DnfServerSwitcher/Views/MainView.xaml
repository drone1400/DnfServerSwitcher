<UserControl x:Class="DnfServerSwitcher.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DnfServerSwitcher.Views"
             xmlns:viewModels="clr-namespace:DnfServerSwitcher.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Grid.Column="0"
               Source="../Resources/DNFSwitcherLogo.png"
               Height="140"
               Margin="6" />

        <Grid Grid.Row="1" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="DNF 2011 System.ini" />
            <Label Grid.Row="1" Grid.Column="0" Content="DNF 2011 Exe" />
            <Label Grid.Row="2" Grid.Column="0" Content="Command Line Args" />
            
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Dnf2011SystemIniPath}" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Dnf2011ExePath}" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Dnf2011ExeCommandLineArgs}" />
            
            <Button Grid.Row="0" Grid.Column="2" Content="Browse"
                    Width="80"
                    Command="{Binding CmdBrowseSystemIni}" />
            <Button Grid.Row="1" Grid.Column="2" Content="Browse"
                    Width="80"
                    Command="{Binding CmdBrowseExe}" />
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Width="220"
                    Height="Auto"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding CmdLaunchNormal}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0"
                           Source="../Resources/button-normal.png"
                           Height="40" Width="40"
                           Margin="6" />
                    <TextBlock Grid.Column="1" Text="Launch Normal" FontSize="20"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Grid>
            </Button>

            <Button Grid.Column="1"
                    Width="220"
                    Height="Auto"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding CmdLaunchDeprecated}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0"
                           Source="../Resources/button-deprecated.png"
                           Height="40" Width="40"
                           Margin="6" />
                    <TextBlock Grid.Column="1" Text="Launch Deprecated" FontSize="18"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Grid>
            </Button>
        </Grid>
        
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <GroupBox Grid.Column="0" Header="Useful Tools"
                      Margin="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.Column="0"
                            Width="220" Height="30"
                            Margin="8"
                            HorizontalAlignment="Center"
                            Content="Get some DNF maps!"
                            Command="{Binding CmdOpenDnfMapsWebsite}" />
                    
                    <Button Grid.Row="1" Grid.Column="0"
                            Width="220" Height="30"
                            Margin="8"
                            HorizontalAlignment="Center"
                            Content="Play a Map!" 
                            Command="{Binding CmdQuickPlayMap}"/>


                </Grid>
            </GroupBox>
            
            <GroupBox Grid.Column="1" Header="Troubleshooting Tools"
                      Margin="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.Column="0"
                            Width="220" Height="30"
                            Margin="8"
                            HorizontalAlignment="Center"
                            Content="Show Help Info"
                            Command="{Binding CmdShowTroubleshootingInfo}" />
                    
                    <Button Grid.Row="1" Grid.Column="0"
                            Width="220" Height="30"
                            Margin="8"
                            HorizontalAlignment="Center"
                            Content="Delete remotecache.vdf file" 
                            Command="{Binding CmdDeleteRemoteCacheVdf}"/>
                    
                    <CheckBox Grid.Row="2" Grid.Column="0"
                              Width="300" Height="30"
                              Margin="8"
                              HorizontalAlignment="Center"
                              VerticalContentAlignment="Center"
                              IsChecked="{Binding EnableSystemIniSteamCloudSync}"
                              Content="Enable updating system.ini in the SteamCloud" />
                </Grid>
            </GroupBox>
        </Grid>

            
    </Grid>
</UserControl>